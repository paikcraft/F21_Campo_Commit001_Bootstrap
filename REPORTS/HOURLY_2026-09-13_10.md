# Relatório horário — 2026-09-13 10:00

## Marco atual

GATE R2 — MVP Manual de Campo.

## Bloco atual

Troca estruturada do banco e preparação para validação física do round-trip.

## Trabalho concluído na última hora

- Adicionado teste de envelope completo com Project, Station, ReferencePoint, Occupation, alturas, evento, artefato e perfil de conexão.
- Corrigida a compatibilidade do teste com a JVM, sem chamar `org.json` Android em teste local.
- Adicionado checklist do teste físico de exportar/importar, fechar/reabrir e conferir os registros.
- Documentado que o JSON preserva metadados e SHA-256 do RAW, mas não transfere os bytes do arquivo.

## Commits

- `0012bdd` — teste inicial de round-trip (corrigido após falha de ambiente).
- `f7e26bc` — teste JVM compatível.
- `64fbebd` — checklist físico do intercâmbio.

## Testes e CI

- GitHub Actions `34749761840`: PASS.
- GitHub Actions `34750232818`: PASS.
- Unit tests: PASS.
- `assembleOperationalDebug`: PASS.
- Upload do APK operationalDebug: PASS.
- Teste físico: ainda não executado nesta janela.

## Limitações e bloqueios

- O Gate R2 não pode ser declarado PASS sem o round-trip físico documentado.
- O intercâmbio atual não copia automaticamente os bytes dos artefatos RAW; importa seus metadados, caminho, tamanho e hash.
- O repositório não possui `gradlew.bat`; a validação local depende do CI.

## Próximo bloco

Executar o checklist no APK operationalDebug, confirmar persistência após fechamento/reabertura e registrar as evidências. Enquanto isso não ocorrer, não iniciar Spectra B1 nem criar commits artificiais.
