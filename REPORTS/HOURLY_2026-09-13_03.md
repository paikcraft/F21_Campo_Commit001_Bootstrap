# Relatório de desenvolvimento — janela iniciada às 03:07

## Implementado nesta janela

- Diretriz de rotina contínua atualizada para verificar o CI a cada 5 minutos e iniciar novo bloco após CI verde.
- Botão de acesso às configurações Bluetooth do Android (`512c77f`).
- Documento inicial do formato de troca JSON e referência aos PDFs F-21/Ficha de Campo (`d368776`).
- Envelope versionado de troca de dados no módulo `data` (`c89ee1a`).
- Snapshot estruturado dos dados persistidos via DAOs (`8ef1cb8`).
- Codec JSON do snapshot (`6284a09`).
- Exportação do banco para arquivo compartilhável (`2dafbbd`).
- Seleção e validação de arquivo JSON de importação (`94e4551`).

## Verificações

Os workflows do GitHub Actions associados aos commits desta janela concluíram com sucesso. Não houve teste físico de exportação/importação nesta janela.

## Limitações atuais

- A importação valida e lê o arquivo, mas ainda não grava as entidades no Room.
- A exportação ainda é JSON estruturado; os PDFs F-21 e Ficha de Campo ainda não são gerados pelo APK.
- Wi-Fi/TCP permanece transporte genérico sem parâmetros ou comandos Spectra presumidos.

## Próxima janela — ordem de prioridade

1. Implementar decodificação dos registros JSON em entidades Room.
2. Gravar importação em transação, com conflito por ID e auditoria.
3. Adicionar testes de round-trip exportar/importar.
4. Preparar renderização dos PDFs usando os exemplos locais como referência visual.
5. Continuar refinando layout operacional e fluxo de conexão de bancada.
